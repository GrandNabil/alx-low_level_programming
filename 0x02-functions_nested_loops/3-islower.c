#include "main.h"

/**
 * _islower - checks for lowercase character
 * @c: the lowercase to check
 * Return: 1 if c is the lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
