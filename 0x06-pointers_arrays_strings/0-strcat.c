#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strcat - concatenates two strings
 * Return: a pointer to the resulting string dest
 */

char *_strcat(char *dest, char *src)
{
	const char *dest = "Hello ";
	const char *src = "World!\n";

	char tab[98];
	strcat(strcpy(buffer, dest), src);
	_putchar(tab[98]);
}

